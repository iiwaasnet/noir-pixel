(function() {
    'use strict';

    angular.module('np.home')
        .controller('HomeController', homeController);

    function homeController(userInfo) {
        var ctrl = this;

        var tmp = userInfo;

    }
})();