{
    "dev": {
        "identity": {
            "server": "mongodb://localhost:27017",
            "database": "np-identity"
        },
        "application": {
            "server": "mongodb://localhost:27017",
            "database": "np"
        }
    }
}
